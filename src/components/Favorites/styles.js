import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  wrapperCars: {
    display: 'flex',
    flexFlow: 'row wrap',
    maxWidth: '1200px',
  },
});

export default useStyles;
